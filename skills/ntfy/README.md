# skill ntfy — Notifications push

Service : ntfy.sh (notifications push mobile)
Usage : alertes heartbeat, notifications hors Telegram
Variable .env requise : `NTFY_TOPIC`

Désactivé par défaut (enabled=False) — ntfy.sh est public.
